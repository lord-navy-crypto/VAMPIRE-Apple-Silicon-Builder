# Security policy

This builder downloads source from the configured upstream repository and compiles it locally. Review `config/build.env` before running if you require a particular audited revision.

Do not post passwords, access tokens, private SSH keys, or other secrets in diagnostics or issue reports.

For installer security issues, open a GitHub issue without including sensitive credentials or private data.
